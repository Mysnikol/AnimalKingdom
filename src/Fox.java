public class Fox extends Canine {

    boolean solitary;
    boolean isOmnivore;

    public void tchoffo(boolean isTchoffing) {
        solitary = true;
        isOmnivore = true;
    }

    public void Prey(String[] args) {
        String prey = new Prey[];
    }

    public speedRange(int max, int min) {
        if (max > min || (max - min + 1 > Integer.MAX_VALUE)) {
            throw new IllegalArgumentException("Invalid Range");
            {

//            int speed =
//            int low = 30, high = 40;

                for (int i = 0; i > 30 && i < 40; i++) {
                    System.out.println(speedRange(high, low));
                }
            }
        }
    }

//    {
//        if (max > min || (max - min + 1 > Integer.MAX_VALUE)) {
//            throw new IllegalArgumentException("Invalid Range");
//        }
//        return new Random().nextInt(max - min + 1) + min;
//
//    }

//        public static void main (String[]args){
//
//        }

    }
}
